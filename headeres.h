#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>
#include <SDL2/SDL_mixer.h>
#include <SDL2/SDL_ttf.h>

#ifndef thehollMenu
#define thehollMenu

void sauv(SDL_Renderer *renderer,SDL_Window *window);
void menuPrincipale(SDL_Renderer *renderer,SDL_Window *window);
void option(SDL_Renderer *renderer,SDL_Window *window);
void jeux(SDL_Renderer *renderer,SDL_Window *window);
void enigme(SDL_Renderer *renderer);
void ghaya(SDL_Renderer *renderer,SDL_Window *window);

#endif
